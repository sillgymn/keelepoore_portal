entity.event_type.date_scheduler:
  path: '/admin/structure/rng/event_types/manage/{event_type}/date_scheduler'
  defaults:
    _entity_form: 'event_type.date_scheduler'
    _title: 'Date scheduler'
  requirements:
    _permission: 'administer event types'
